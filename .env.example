# --- Application ---
PORT=8000
DEBUG=True
SECRET_KEY=replace_with_random_string

# --- Database (Local Docker mode) ---
POSTGRES_DB=ctm_db
POSTGRES_USER=ctm_user
POSTGRES_PASSWORD=replace_with_strong_password
POSTGRES_HOST=db
POSTGRES_PORT=5433   # standardised external port mapping

# --- App runtime ---
HOST=0.0.0.0
RELOAD=True

# --- Database (Cloud / Shared DB mode) ---
# Uncomment this block and comment out the Local Docker block above when connecting
# to a cloud instance or shared Postgres.
# POSTGRES_DB=ctm_db
# POSTGRES_USER=team_user
# POSTGRES_PASSWORD=change_me
# POSTGRES_HOST=your-cloud-db-host.rds.amazonaws.com
# POSTGRES_PORT=5432
# DEBUG=False
# RELOAD=False

