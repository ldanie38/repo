// Placeholder service worker for future jobs (prospecting, messaging, etc.)
chrome.runtime.onInstalled.addListener(() => {
  console.log('FACRM extension installed (Base Project Scaffold scaffold).');
});
